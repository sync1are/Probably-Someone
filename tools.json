{
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "take_screenshot",
        "description": "Captures a screenshot of the entire screen. Use this when the user asks what's on their screen, to see their screen, or to help with something visible.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "get_clipboard",
        "description": "Get the current clipboard content. Use when user says 'what did I copy', 'what's in my clipboard', or 'summarize what I copied'.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "set_clipboard",
        "description": "Copy text to the clipboard. Use when user says 'copy this', 'save to clipboard'.",
        "parameters": {
          "type": "object",
          "properties": {
            "text": {
              "type": "string",
              "description": "The text to copy to clipboard"
            }
          },
          "required": ["text"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "get_active_window",
        "description": "Get the title and app name of the currently active window. Use when user asks about their current app.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "scrape_webpage",
        "description": "Extract text content from a webpage URL. Use when user wants to summarize an article or read web content.",
        "parameters": {
          "type": "object",
          "properties": {
            "url": {
              "type": "string",
              "description": "The URL of the webpage to scrape"
            }
          },
          "required": ["url"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "spotify_play",
        "description": "Play a song, artist, playlist, or album on Spotify. Use when user asks to play music.",
        "parameters": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Search query for content to play"
            },
            "content_type": {
              "type": "string",
              "enum": ["track", "playlist", "album", "artist"],
              "description": "Type of content"
            }
          },
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "spotify_pause",
        "description": "Pause Spotify playback.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "spotify_skip_next",
        "description": "Skip to next track.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "spotify_skip_previous",
        "description": "Go back to previous track.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "spotify_add_to_queue",
        "description": "Add a song to queue.",
        "parameters": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Track to add"
            }
          },
          "required": ["query"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "spotify_current_track",
        "description": "Get the name, artist, and album of the currently playing track. Use when user asks 'what song is this', 'what's playing', 'song name', 'current song', or 'what am I listening to'.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "spotify_shuffle",
        "description": "Enable or disable shuffle.",
        "parameters": {
          "type": "object",
          "properties": {
            "state": {
              "type": "boolean",
              "description": "True for on, False for off"
            }
          },
          "required": ["state"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "spotify_repeat",
        "description": "Set repeat mode.",
        "parameters": {
          "type": "object",
          "properties": {
            "state": {
              "type": "string",
              "enum": ["track", "context", "off"]
            }
          },
          "required": ["state"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "spotify_volume",
        "description": "Set volume level.",
        "parameters": {
          "type": "object",
          "properties": {
            "volume_percent": {
              "type": "integer",
              "minimum": 0,
              "maximum": 100
            }
          },
          "required": ["volume_percent"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "spotify_like_current",
        "description": "Like current song.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "spotify_unlike_current",
        "description": "Unlike current song.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "setup_whatsapp",
        "description": "Setup WhatsApp auto-reply integration for the FIRST TIME. Shows QR code for authentication. ONLY use if user explicitly asks to 'setup WhatsApp', 'connect WhatsApp', or 'link WhatsApp'. Do NOT use for sending messages.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "setup_discord",
        "description": "Setup Discord auto-reply integration. Guides user through getting Discord token. Use when user asks to setup Discord, connect Discord, or enable Discord messaging.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "start_messaging",
        "description": "Start the messaging system (WhatsApp and/or Discord). Use when user asks to start messaging, activate auto-reply, or turn on message responses.",
        "parameters": {
          "type": "object",
          "properties": {
            "platform": {
              "type": "string",
              "enum": ["whatsapp", "discord", "both"],
              "description": "Which platform to start"
            }
          },
          "required": ["platform"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "stop_messaging",
        "description": "Stop the messaging system. Use when user asks to stop messaging, deactivate auto-reply, or turn off message responses.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "messaging_status",
        "description": "Check messaging system status and statistics. Use when user asks about messaging status, how many messages sent, or if messaging is active.",
        "parameters": {
          "type": "object",
          "properties": {},
          "required": []
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "add_messaging_contact",
        "description": "Add a contact to the messaging whitelist. Use when user wants to add someone to auto-reply list.",
        "parameters": {
          "type": "object",
          "properties": {
            "platform": {
              "type": "string",
              "enum": ["whatsapp", "discord"],
              "description": "Platform for the contact"
            },
            "contact": {
              "type": "string",
              "description": "Phone number for WhatsApp or user ID for Discord"
            }
          },
          "required": ["platform", "contact"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "manage_whitelist",
        "description": "Manage messaging whitelist with natural language. Use for: listing contacts, removing contacts, clearing whitelist. Examples: 'show whitelist', 'remove John from WhatsApp', 'clear Discord whitelist', 'who's on the list'.",
        "parameters": {
          "type": "object",
          "properties": {
            "action": {
              "type": "string",
              "enum": ["add", "remove", "list", "clear"],
              "description": "Action to perform: add, remove, list, or clear"
            },
            "platform": {
              "type": "string",
              "enum": ["whatsapp", "discord"],
              "description": "Platform (required for add/remove/clear, optional for list)"
            },
            "contact": {
              "type": "string",
              "description": "Contact identifier (required for add/remove)"
            }
          },
          "required": ["action"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "send_message",
        "description": "Send a message to a contact on WhatsApp or Discord. Use when user wants to send a message like 'send hey to John', 'message Mom on WhatsApp', 'text Anas hello'.",
        "parameters": {
          "type": "object",
          "properties": {
            "platform": {
              "type": "string",
              "enum": ["whatsapp", "discord"],
              "description": "Platform to send on"
            },
            "contact": {
              "type": "string",
              "description": "Contact name or phone number"
            },
            "message": {
              "type": "string",
              "description": "Message to send"
            }
          },
          "required": ["platform", "contact", "message"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "get_last_message",
        "description": "Get the last message received from a contact on WhatsApp or Discord. Use when user asks 'what did they say', 'read last message from X', 'show messages from Y', 'what was the last message'.",
        "parameters": {
          "type": "object",
          "properties": {
            "platform": {
              "type": "string",
              "enum": ["whatsapp", "discord"],
              "description": "Platform to check"
            },
            "contact": {
              "type": "string",
              "description": "Contact name or number (optional - if not provided, returns last message from anyone)"
            }
          },
          "required": ["platform"]
        }
      }
    }
  ]
}
